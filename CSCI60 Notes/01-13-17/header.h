#ifndef HEADER_H
#define HEADER_H
// never use using namespace std in header
// by separating things in files, u can do things in parallel
#endif // old style language that starts code with "if" and ends with "if"
